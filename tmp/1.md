


```sh

```



```sh

chcp 65001

e:
cd E:\tool\pgsql\bin
set PGCLIENTENCODING=UTF8
set PGDATA=D:\pgdata

# 初始化 数据目录
pg_ctl initdb

pg_ctl start

pg_ctl status

# 登录数据库
psql -d postgres 

```


```sql
CREATE DATABASE musicbrainz;

\c musicbrainz

CREATE TABLE albums (
    id SERIAL PRIMARY KEY,
    release_id VARCHAR(255) NOT NULL DEFAULT '',
    catalognumber VARCHAR(255) NOT NULL DEFAULT '',
    date VARCHAR(255) NOT NULL DEFAULT '',
    album TEXT NOT NULL DEFAULT '',
    tracks TEXT NOT NULL DEFAULT '',
    themes TEXT[] NOT NULL DEFAULT '{}',
    links TEXT[] NOT NULL DEFAULT '{}'
);

-- 添加索引
CREATE INDEX idx_albums_release_id ON albums(release_id);


COPY albums (release_id, catalognumber, date, album, tracks, themes, links)
FROM 'D:\\1.csv' 
WITH (
    FORMAT csv,
    HEADER true,
    DELIMITER ',',
    QUOTE '"',
    NULL '',
    ENCODING 'UTF-8'
);


SELECT count(*) FROM albums;

SELECT count(*) FROM albums;

DROP TABLE albums;

SELECT * FROM albums
WHERE random() < 0.01
LIMIT 10;


```



## 备份恢复

```sh
pg_dump -Fc -f d:/musicbrainz.dump musicbrainz

pg_restore -h hostname -U username -d musicbrainz musicbrainz.dump
```








